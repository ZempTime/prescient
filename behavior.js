// Pseudocode of all the behavior we'll need to implement

const behaviors = {
  authenticatable,
  confirmable,
  lockable,
  omniauthable, // ehhh
  recoverable,
  registerable,
  rememberable,
  timeoutable,
  trackable,
  validatable
}

// multiple devices?
//

const views = {
  confirmations,
  passwords,
  registrations,
  sessions,
  unlocks,
}
